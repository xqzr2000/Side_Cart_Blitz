# Required for Mom + Bestie chat
OPENROUTER_API_KEY=

# Stable default; OpenRouter chooses an appropriate model.
# Bestie calls tools, so pick a model with solid function-calling support.
OPENROUTER_MODEL=openrouter/auto

# Optional. Lets Bestie check live prices through OpenRouter's web plugin
# instead of relying only on the built-in reference cost library.
OPENROUTER_WEB_SEARCH=false

# Backend server
PORT=8787
HOST=0.0.0.0

# Optional. Recommended when you expose the backend on a public Codespaces URL
# or deploy it to another machine. Put the same value in the extension Options page.
APP_SHARED_SECRET=

# Optional OpenRouter attribution headers
APP_URL=http://localhost:8787
APP_TITLE=CartSide Blitz
