{
  "name": "wiseshelf-side-cart",
  "version": "1.0.0",
  "private": true,
  "description": "Production-grade WiseShelf browser side-panel budgeting companion with optional TypeSafe Jev decision support.",
  "scripts": {
    "dev": "node --watch backend/server.js",
    "start": "node backend/server.js",
    "check": "node --check backend/*.js && node --check extension/*.js",
    "test": "node --test backend/*.test.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
